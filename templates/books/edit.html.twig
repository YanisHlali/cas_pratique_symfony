{% extends 'base.html.twig' %}

{% block title %}
	Modification du livre
	{{ book.title }}
	{{ parent() }}
{% endblock %}

{% block body %}
	<section class="container mt-4">
		<h1 class="text-center">Modification du livre</h1>
		{% include '_form.html.twig' with {buttonLabel: 'Modifier'} %}
		<a href="{{ path('app_books') }}" class="btn btn-primary mt-4">Retour à la liste</a>
	</section>
{% endblock %}
