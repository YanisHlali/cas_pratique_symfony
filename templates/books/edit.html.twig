{% extends 'base.html.twig' %}

{% block title %}
	Modification du livre
	{{ book.title }}
	{{ parent() }}
{% endblock %}

{% block body %}
    {{ dump(book)}}
	<section class="container mt-4">
		<h1 class="text-center">Modification du livre - {{ book.id }}</h1>
		{% include 'books/_form.html.twig' with {buttonLabel: 'Modifier'} %}
		<a href="{{ path('app_books') }}" class="btn btn-primary mt-4">Retour à la liste</a>
	</section>
{% endblock %}
